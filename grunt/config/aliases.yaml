# Grunt aliases
---
# Build everything
build:
  - 'build:js'
# Build JavaScript from assets to production
'build:js':
  - 'clean'
  - 'uglify'
  - 'concat'
# Check health of the project
check:
  - 'jshint'
  - 'jsonlint'
  - 'jsvalidate'
# Default task
default:
  - check
  - build
