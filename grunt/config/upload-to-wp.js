/**
 * ...
 *
 * @param {Object} grunt The grunt helper object.
 * @returns {void}
 */
module.exports = function( grunt ) {
	grunt.registerTask(
		"upload-to-wp",
		"Checks for the stable tag and deploy to WordPress so translation can start",
		function() {
		}
	);
};
