/**
 * Exports the main AppProvider component that wraps the entire redirects application
 * with necessary providers (routing, styling isolation, slot/fill).
 * This module is designed to be consumed by premium through the
 * editor-modules system as `window.yoast.editorModules.components.AppRedirectsProvider`.
 */

export { AppProvider } from "./appProvider";
export { RouteLayout } from "./components";
