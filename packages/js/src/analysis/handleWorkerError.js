
/**
 * Dispatches a warning message if the SEO or readability analysis fails.
 *
 * **Warning**: has not been implemented yet.
 *
 * @returns {void}
 */
export default function handleWorkerError() {
	/*
	 * This function should be implemented to catch and handle errors
	 * in the analysis web worker.
	 *
	 * For your information: the line below can be used to dispatch a warning message. Where the
	 * warning message is a React component (for example made by `interpolateComponents`).
	 * ```js
	 *    window.YoastSEO.store.dispatch( setWarningMessage( message ) );
	 * ```
	 */
}
