import SlotWithDefault from "./slots/SlotWithDefault";

/**
 * The AiImageGenerator component.
 *
 * @returns {wp.Element} The AiImageGenerator component.
 */
export const AiImageGenerator = () => {
	return (
		<SlotWithDefault  name="AiImageGenerator">
			<p> Premium needed</p>
		</SlotWithDefault>
	);
};
