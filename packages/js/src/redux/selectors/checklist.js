/**
 * Returns the check list items.
 *
 * @param {Object} state The state.
 *
 * @returns {bool} The check list items.
 */
export function getChecklistItems( state ) {
	return state.checklistItems;
}
