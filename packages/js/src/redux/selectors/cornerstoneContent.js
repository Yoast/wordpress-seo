/**
 * Returns whether the current content is cornerstone content.
 *
 * @param {Object} state The state.
 *
 * @returns {bool} Whether the current content is cornerstone content.
 */
export function isCornerstoneContent( state ) {
	return state.isCornerstone;
}
