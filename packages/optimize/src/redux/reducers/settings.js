/**
 * A reducer for the settings in the state.
 *
 * @param {Object} state The state.
 * @param {Object} action The action.
 *
 * @returns {Object} The new state.
 */
export default function settingsReducer( state, { type } ) {
	switch ( type ) {
		default:
			return state;
	}
}
