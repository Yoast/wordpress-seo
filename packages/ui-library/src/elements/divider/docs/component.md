The Divider element is a thin horizontal line that separates content into groups, helping users scan a layout by visually grouping related items.

By default, a divider is a single line. Passing `children` splits the line and centers the content between its two halves, which is useful for a labeled separator or an inline control such as a "Show more" toggle.
