Each tab drives its own panel — a detail panel, a table, or a spec sheet — to show the content area isn't tied to a single layout. One `ContentTabs.Panel` per tab is rendered inside `ContentTabs.Content`; each panel shows itself when its `tabId` matches the active tab in context and cross-fades with the others. No `isSelected` is computed by hand and no external selected-product state is needed.
