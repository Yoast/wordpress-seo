/**
 * Returns a list of the participles used for the Greek passive voice assessment.
 * Technically, in Greek this form is called "infinitive".
 * Some infinitives use the verb "to have" as an auxiliary, some others use the verb "to be".
 *
 * @returns {Array}                The list with participles.
 */
export default infinitivesUsingToHave = [
];

export default infinitivesUsingToBe = [
];
