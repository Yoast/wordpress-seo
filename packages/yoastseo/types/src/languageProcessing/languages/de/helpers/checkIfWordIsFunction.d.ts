/**
 * Checks if a word is a function word.
 *
 * @param {string} word The word to check.
 *
 * @returns {boolean} Whether or not a word is a function word.
 */
export default function checkIfWordIsFunction(word: string): boolean;
