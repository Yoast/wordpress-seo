{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/grunt-*",
      "**/eslint*"
    ]
  },
  "scripts": {
    "lint": "lerna run lint",
    "test": "lerna run test --stream",
    "install-ci": "lerna run install-ci"
  }
}
