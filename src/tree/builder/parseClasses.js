/**
 * The html-elements that are irrelevant for further analysis.
 * @type {string[]}
 */
export const irrelevantTypes = [ "code", "style", "link" ];

/**
 * The html-element that should be parsed as formatting elements ("phrasing content").
 * @type {string[]}
 */
export const formattingTypes = [ "a", "b", "emph" ];

