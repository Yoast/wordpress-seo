const HowToConfiguration = require( "./configuration-how-to.json" );
const FAQConfiguration = require( "./configuration-faq.json" );

// Mapping of path => configuration.
module.exports = {
	"how-to": HowToConfiguration,
	faq: FAQConfiguration,
};
