/**
 * Gets isOpen from the state.
 *
 * @param {Object} state The state.
 *
 * @returns {boolean} Whether the post settings modal is open or not.
 */
export const getPostSettingsModalIsOpen = state => state.postSettingsModal.isOpen;
